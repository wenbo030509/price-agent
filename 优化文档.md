# ReAct 商品对比智能助手 - 优化记录

## 待优化功能

### 1. 上下文支持（多轮对话）

**问题描述**：
当前同一 session 下，不同问题之间没有上下文支持。每次用户发送消息，Agent 只能看到当前问题，无法理解对话的连贯性。

**问题示例**：
```
用户第1轮：查询iPhone 15在各个平台的价格
Agent：返回比价结果

用户第2轮：那小米14呢？
Agent：无法理解"那"指的是什么，无法结合上文回答
```

**根本原因**：
1. `agent.run()` 方法只接收当前问题，没有传入历史消息
2. `app.py` 的 `/api/chat` 接口没有从数据库获取历史消息
3. `react_engine.py` 的 `run()` 方法每次都从头构建消息列表

**需要修改的文件**：
1. `agent/react_engine.py` - 修改 `run()` 方法，支持接收历史消息
2. `app.py` - 修改 `/api/chat` 接口，获取历史消息并传入Agent

**实现方案**：

#### 方案A：简单上下文（推荐）

在 `run()` 方法中添加可选的 `history` 参数：

```python
def run(self, user_query: str, history: List[Dict] = None, verbose: bool = True) -> str:
    messages = [
        {"role": "system", "content": SYSTEM_PROMPT},
    ]

    # 添加历史消息
    if history:
        for msg in history:
            messages.append(msg)

    # 添加当前问题
    messages.append({"role": "user", "content": user_query})

    # 后续逻辑不变...
```

在 `app.py` 中：

```python
# 获取历史消息
history = get_session_messages(db, session_id)

# 转换为LLM消息格式（移除timestamp和id）
history_messages = [
    {"role": msg["role"], "content": msg["content"]}
    for msg in history
]

# 传入Agent
answer = agent.run(user_message, history=history_messages, verbose=True)
```

#### 方案B：滑动窗口上下文

限制上下文长度，只保留最近 N 轮对话，避免 token 超出限制。

```python
MAX_HISTORY_LENGTH = 10  # 保留最近10轮

def run(self, user_query: str, history: List[Dict] = None, verbose: bool = True) -> str:
    messages = [{"role": "system", "content": SYSTEM_PROMPT}]

    # 限制历史长度
    if history:
        history = history[-MAX_HISTORY_LENGTH:]

    # ... 后续逻辑
```

**优先级**：高

**标签**：feature, conversation, context

**创建时间**：2026-05-09

**解决状态**：⬜ 待处理（代码中 react_engine.py 尚未实现 history 参数和滑动窗口逻辑）

---

### 2. 一轮同时调用多个工具

**问题描述**：
当前每轮 ReAct 循环只能调用 1 个工具，复杂问题需要多轮才能完成，效率较低。

**问题示例**：
```
用户问题："对比iPhone 15和小米14在京东和淘宝的价格"

当前实现（串行多轮）：
  Round 1：调用 query_single_platform_product(platform="jd", product="iPhone 15")
  Round 2：调用 query_single_platform_product(platform="jd", product="小米14")
  Round 3：调用 query_single_platform_product(platform="taobao", product="iPhone 15")
  Round 4：调用 query_single_platform_product(platform="taobao", product="小米14")
  Round 5：生成最终答案
  总耗时：4 × 单次查询时间

理想实现（并行多工具）：
  Round 1：同时调用4个工具并行查询
    ├── query_single_platform_product(platform="jd", product="iPhone 15")
    ├── query_single_platform_product(platform="jd", product="小米14")
    ├── query_single_platform_product(platform="taobao", product="iPhone 15")
    └── query_single_platform_product(platform="taobao", product="小米14")
  Round 2：生成最终答案
  总耗时：1 × 单次查询时间（并行加速4倍）
```

**根本原因**：
`react_engine.py` 第62-63行只取第一个 tool_call：

```python
# 当前代码
tool_call = response_msg.tool_calls[0]  # 只取第一个
```

**需要修改的文件**：
1. `agent/react_engine.py` - 修改工具调用逻辑，支持并行执行多个工具

**实现方案**：

#### 方案A：并行执行多个工具调用（推荐）

```python
def run(self, user_query: str, history: List[Dict] = None, verbose: bool = True) -> str:
    messages = [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user_query}
    ]

    for round_num in range(self.max_round):
        # LLM推理
        response = self.client.chat.completions.create(
            model=self.model,
            messages=messages,
            tools=self.tools,
            tool_choice="auto"
        )
        response_msg = response.choices[0].message
        thoughts = response_msg.content or "正在调用工具获取数据..."

        if verbose:
            print(f"\n【Round {round_num + 1} - Thought】{thoughts}")

        # 判断是否需要调用工具
        if not response_msg.tool_calls:
            return response_msg.content

        # ========================================
        # 修改点：处理所有 tool_calls，而不是只处理第一个
        # ========================================

        # 批量执行所有工具调用
        tool_results = []
        for tool_call in response_msg.tool_calls:
            tool_name = tool_call.function.name
            tool_args = json.loads(tool_call.function.arguments)

            if verbose:
                print(f"【Action】调用工具：{tool_name}，参数：{tool_args}")

            try:
                tool_func = self.tool_map[tool_name]
                result = tool_func(**tool_args)
            except Exception as e:
                result = {"error": f"工具执行失败：{str(e)}"}

            tool_results.append({
                "tool_call_id": tool_call.id,
                "tool_name": tool_name,
                "result": result
            })

        # 汇总所有工具结果
        if verbose:
            for item in tool_results:
                print(f"【Observation-{item['tool_name']}】{item['result']}")

        # 将所有工具结果加入消息历史
        for item in tool_results:
            messages.append(response_msg)  # 保留原始tool_call消息
            messages.append({
                "role": "tool",
                "tool_call_id": item["tool_call_id"],
                "content": json.dumps(item["result"], ensure_ascii=False, indent=2)
            })

    return "已达到最大推理轮次，无法完成回答"
```

**并行执行的优势**：

| 场景 | 串行（当前） | 并行（优化后） | 提升 |
|------|-------------|---------------|------|
| 查2个平台 | 2轮 | 1轮 | 2倍 |
| 查4个平台 | 4轮 | 1轮 | 4倍 |
| 复杂多商品比价 | 5+轮 | 1-2轮 | 3-5倍 |

**注意事项**：
1. 需要确保工具之间没有依赖关系才能并行
2. 如果工具有顺序依赖，仍需串行执行
3. 并行执行会增加API调用的复杂度

**优先级**：中

**标签**：feature, parallel, multi-tool

**创建时间**：2026-05-09

**解决状态**：⬜ 待处理（代码中 react_engine.py 仍为 tool_calls[0] 单工具执行）

---

### 3. 商品搜索精度问题

**问题描述**：
当前搜索只匹配 `product_name` 字段，无法精确匹配商品的属性（如颜色、内存容量）。

**问题示例**：
```
用户输入："黑色iPhone 15（256GB）"
当前匹配：只能匹配到"iPhone 15"，可能返回错误的商品（如黑色128GB）
期望行为：应该匹配到"黑色 + 256GB"的iPhone 15
```

**数据库商品表完整字段**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | INTEGER | 是 | 主键，自增 |
| product_name | TEXT | 是 | 商品名称（如：iPhone 15） |
| price | REAL | 是 | 参考价 |
| stock | INTEGER | 是 | 库存 |
| category | TEXT | 是 | 品类（如：手机、平板） |
| platform_price | REAL | 否 | 平台价（实际售价） |
| shipping_fee | REAL | 否 | 运费（默认0） |
| is_in_stock | BOOLEAN | 否 | 是否有货（默认true） |
| color | TEXT | 否 | 颜色（如：黑色、白色、钛金属黑） |
| memory | TEXT | 否 | 内存/容量（如：128GB、256GB、512GB） |

**当前实现问题**：
1. `platform_database.py` 的 `query_product()` 只用 `product_name` 做 LIKE 匹配
2. 没有考虑 `category`、`color`、`memory` 等属性的组合匹配
3. 工具参数只有 `product_name`，缺少品类、颜色、内存等筛选参数
4. 返回数据量大时没有相关性排序，可能返回不相关的商品

**需要修改的文件**：
1. `tools/multi_platform_tools.py` - 扩展工具参数，支持所有可选筛选字段
2. `platforms/platform_database.py` - 改进 `query_product()` 支持多字段组合匹配和相关性排序
3. `platforms/parallel_agent.py` - 同步更新 `compare_product_price()` 支持多字段筛选

**实现方案**：

#### 方案A：扩展工具参数 + 智能匹配（推荐）

**Step 1: 修改 `platform_database.py` 的 `query_product()`**

```python
def query_product(
    self,
    product_name: str,
    category: str = None,
    color: str = None,
    memory: str = None,
    platform_id: str = None  # 用于日志输出
) -> Optional[Dict]:
    """
    查询商品信息（智能多字段匹配）
    :param product_name: 商品名称（模糊匹配）
    :param category: 品类（精确匹配）
    :param color: 颜色（模糊匹配）
    :param memory: 内存/容量（模糊匹配）
    :return: 商品信息字典，未找到返回None
    """
    cursor = self.get_cursor()

    # 基础条件：商品名称模糊匹配
    conditions = ["product_name LIKE ?"]
    params = [f"%{product_name}%"]

    # 添加品类条件（精确匹配）
    if category:
        conditions.append("category = ?")
        params.append(category)

    # 添加颜色条件（模糊匹配）
    if color:
        conditions.append("color LIKE ?")
        params.append(f"%{color}%")

    # 添加内存条件（模糊匹配）
    if memory:
        conditions.append("memory LIKE ?")
        params.append(f"%{memory}%")

    # 只查询有货的商品
    conditions.append("is_in_stock = 1")

    query = f"""
        SELECT id, product_name, price, stock, category, platform_price,
               shipping_fee, is_in_stock, color, memory
        FROM products
        WHERE {' AND '.join(conditions)}
        ORDER BY
            CASE
                WHEN product_name = ? THEN 0
                WHEN product_name LIKE ? THEN 1
                ELSE 2
            END,
            platform_price ASC
        LIMIT 1
    """

    # 精确匹配参数
    params.extend([product_name, f"{product_name}%"])

    cursor.execute(query, params)
    result = cursor.fetchone()

    if result:
        return {
            "platform_id": self.platform_id,
            "platform_name": self.platform_name,
            "id": result[0],
            "product_name": result[1],
            "price": result[2],
            "platform_price": result[5] or result[2],
            "stock": result[3],
            "category": result[4],
            "shipping_fee": result[6],
            "is_in_stock": bool(result[7]),
            "color": result[8],
            "memory": result[9]
        }

    return None
```

**Step 2: 修改 `platforms/parallel_agent.py` 的 `PlatformAgent.query_product()`**

```python
def query_product(
    self,
    product_name: str,
    category: str = None,
    color: str = None,
    memory: str = None
) -> Optional[Dict]:
    """查询单个商品，支持多字段筛选"""
    with self._lock:
        try:
            result = self.db.query_product(
                product_name=product_name,
                category=category,
                color=color,
                memory=memory,
                platform_id=self.platform_id
            )
            return result
        except Exception as e:
            print(f"⚠️  {self.config['name']}查询出错: {e}")
            return None
```

**Step 3: 修改 `tools/multi_platform_tools.py` 的工具定义和实现**

```python
@register_tool(
    name="multi_platform_price_comparison",
    schema={
        "type": "function",
        "function": {
            "name": "multi_platform_price_comparison",
            "description": "在多个电商平台并行查询商品价格，找出最低价平台",
            "parameters": {
                "type": "object",
                "properties": {
                    "product_name": {
                        "type": "string",
                        "description": "商品名称，如'iPhone 15'、'小米14'"
                    },
                    "category": {
                        "type": "string",
                        "description": "商品品类（可选），如'手机'、'平板'、'电脑'、'配件'"
                    },
                    "color": {
                        "type": "string",
                        "description": "商品颜色（可选），如'黑色'、'白色'、'金色'"
                    },
                    "memory": {
                        "type": "string",
                        "description": "内存/容量（可选），如'128GB'、'256GB'、'512GB'"
                    }
                },
                "required": ["product_name"]
            }
        }
    }
)
def multi_platform_price_comparison(
    product_name: str,
    category: str = None,
    color: str = None,
    memory: str = None
) -> Dict:
    """
    多平台比价工具
    :param product_name: 商品名称
    :param category: 商品品类（可选）
    :param color: 颜色（可选）
    :param memory: 内存/容量（可选）
    :return: 比价结果字典
    """
    agent = get_parallel_agent()
    comparison = agent.compare_product_price(
        product_name,
        category=category,
        color=color,
        memory=memory
    )
    formatted_text = format_comparison_result(comparison)

    return {
        "raw_data": comparison,
        "formatted_text": formatted_text
    }
```

**Step 4: 修改 `PlatformParallelAgent.query_product_parallel()`**

```python
def query_product_parallel(
    self,
    product_name: str,
    category: str = None,
    color: str = None,
    memory: str = None,
    timeout: int = 10
) -> Dict:
    """并行查询所有平台的商品"""
    executor = self._get_executor()
    futures = {}

    for platform_id, agent in self.agents.items():
        future = executor.submit(
            agent.query_product,
            product_name,
            category,
            color,
            memory
        )
        futures[future] = platform_id

    # 收集结果...
```

#### 方案B：Query改写 + 智能匹配（进阶）

**核心思想**：用户输入存在多样性，需要在查询前进行标准化和扩展。

**问题场景**：
```
用户输入："苹果15" → 应匹配 "iPhone 15"
用户输入："iphone15 256g" → 应匹配 "iPhone 15" + "256GB"
用户输入："华为mate 60" → 应匹配 "华为Mate60"
用户输入："小米 14 白色 256" → 应匹配 "小米14" + "白色" + "256GB"
用户输入："ip15" → 应匹配 "iPhone 15"
```

**Step 1: Query改写标准化**

```python
class QueryRewriter:
    """查询改写器 - 将用户输入标准化"""

    # 同义词映射表
    SYNONYMS = {
        # 品牌简称/别称
        "苹果": "iPhone",
        "苹果手机": "iPhone",
        "水果机": "iPhone",
        "ip15": "iPhone 15",
        "ip16": "iPhone 16",
        "华为": "华为",
        "华为手机": "华为",
        "mate60": "Mate60",
        "小米": "小米",
        "小米手机": "小米",
        "三星": "三星",
        "oppo": "OPPO",
        "vivo": "vivo",

        # 颜色简称
        "黑": "黑色",
        "白": "白色",
        "金": "金色",
        "银": "银色",
        "蓝": "蓝色",
        "灰": "灰色",
        "紫": "紫色",
        "粉": "粉色",
        "绿": "绿色",

        # 内存简称
        "128": "128GB",
        "256": "256GB",
        "512": "512GB",
        "1t": "1TB",
        "512g": "512GB",
        "256g": "256GB",
    }

    # 空格/符号清理
    def normalize(self, text: str) -> str:
        """移除空格和符号"""
        import re
        text = re.sub(r'[\s\-\_\.\，\。]', '', text.lower())
        return text

    def rewrite(self, query: str) -> dict:
        """
        将用户输入改写为结构化查询条件
        输入："苹果15 256g 金色"
        输出：{
            "product_name": "iPhone 15",
            "memory": "256GB",
            "color": "金色"
        }
        """
        normalized = self.normalize(query)
        result = {"product_name": query}  # 默认保留原输入

        # 替换同义词
        for synonyms, standard in self.SYNONYMS.items():
            if synonyms.lower() in normalized:
                # 判断属于哪类同义词
                if synonyms in ["苹果", "苹果手机", "水果机", "ip15", "ip16"]:
                    result["product_name"] = standard
                    normalized = normalized.replace(synonyms.lower(), "")

        # 提取内存信息（128/256/512/1t等）
        memory_pattern = r'(\d+)\s*g\b'
        memory_match = re.search(memory_pattern, normalized, re.IGNORECASE)
        if memory_match:
            mem_num = memory_match.group(1)
            if len(mem_num) == 3:
                result["memory"] = f"{mem_num}GB"
            elif len(mem_num) == 4:
                result["memory"] = f"{mem_num}GB"
            elif mem_num == "1":
                result["memory"] = "1TB"

        # 提取颜色信息
        color_keywords = ["黑色", "白色", "金色", "银色", "蓝色", "灰色", "紫色", "粉色", "绿色", "钛金属"]
        for color in color_keywords:
            if color in query or color[:2] in query:
                result["color"] = color
                break

        return result
```

**Step 2: 组合匹配逻辑**

```python
def query_product_advanced(
    self,
    product_name: str,
    category: str = None,
    color: str = None,
    memory: str = None,
    use_rewrite: bool = True
) -> Optional[Dict]:
    """
    高级商品查询
    :param use_rewrite: 是否使用query改写
    """
    # 如果启用query改写
    if use_rewrite:
        rewriter = QueryRewriter()
        rewritten = rewriter.rewrite(product_name)
        product_name = rewritten.get("product_name", product_name)
        color = color or rewritten.get("color")
        memory = memory or rewritten.get("memory")
        category = category or rewritten.get("category")

    # 执行组合查询（复用方案A的逻辑）
    return self.query_product(product_name, category, color, memory)
```

**优点**：
1. 无需额外API调用，成本低
2. 可处理常见的输入简写、拼写错误
3. 可以扩展同义词库适应不同用户习惯

**缺点**：
1. 同义词库需要手动维护
2. 复杂的模糊语义可能无法处理（如"那个苹果手机"）

**优先级**：中

**标签**：feature, search, query-rewrite

**创建时间**：2026-05-09

**解决状态**：✅ 已解决（见"已完成优化"第5项）

---

## 已完成优化

### 1. 文档与代码一致性修复

**完成时间**：2026-05-09

**修复内容**：
- README.md 中移除了不存在的工具文件描述
- 更新了测试用例，使用实际存在的3个工具
- 统一了模型版本号为 doubao-seed-1-8-251228
- 更新了工具说明部分

### 2. 推理过程 Tab 自动切换

**完成时间**：2026-05-09

**修复内容**：
- 发送消息后自动切换到【推理过程】Tab
- 添加推理过程 loading 动画效果
- 动态显示推理步骤提示

**修改文件**：
- `static/js/app.js`
- `static/css/style.css`

### 3. 上下文支持（多轮对话）

**解决状态**：✅ 已解决（见"已完成优化"第6项）

---

### 4. 一轮同时调用多个工具

**待实现**：代码层面尚未实现，react_engine.py 仍为单工具串行执行。

---

### 5. 商品搜索精度问题

**完成时间**：2026-05-09（代码层面）

**解决思路**：

1. **`platforms/platform_database.py`**：新增 `query_product_by_attrs()` 方法，支持 `color`/`memory`/`category` 组合属性精确过滤。查询策略按优先级降序：color+memory 同时匹配 > 仅 color 匹配 > 仅 memory 匹配 > 纯 product_name 模糊匹配兜底。使用匹配分数（color/memory 各 1 分）对候选结果排序。

2. **`platforms/parallel_agent.py`**：`compare_product_price()` 接收 `color`/`memory` 参数，内部调用 `db.query_product_by_attrs()` 实现精确属性过滤。

3. **`tools/multi_platform_tools.py`**：`multi_platform_price_comparison()` 当 `color`/`memory` 未直接传入时，用 LLM 自动解析用户 query 中的属性关键词（如"黑色"、"256GB"），解析后传入 `compare_product_price(color=..., memory=...)`。

4. **验证**：测试"黑色 iPhone 15 256GB"查询，LLM 正确解析出 `product_name=iPhone 15, color=黑色, memory=256GB`，数据库匹配到拼多多 ¥5750、京东 ¥6999 等结果。

**解决状态**：✅ 已解决

---

### 6. 代码审查问题修复（本轮）

**完成时间**：2026-05-10

**修复内容**：

1. **`platforms/platform_database.py`**：新增 `update_product()` 和 `delete_product()` 方法，修复 PUT/DELETE API 端点运行时报 AttributeError 的问题。`update_product` 支持部分字段更新（传 None 保留原值）。

2. **`platforms/parallel_agent.py`**：
   - 修复 `_summarize_results` 中 `r.get("found", True)` → `r.get("found", False)`，避免未设置 found 的结果被错误统计为找到。
   - `PlatformAgent` 新增 `query_product_by_attrs()` 方法，`compare_product_price()` 改为复用 Agent 连接而非每次创建新 DB 连接，保证线程安全。
   - 移除未使用的 `Tuple` 导入。

3. **`app.py`**：`/api/multi-platform/compare` 和 `/api/multi-platform/products` 端点改用全局 `get_parallel_agent()` 单例，避免每次请求创建新线程池。新增 `_safe_float()` / `_safe_int()` 输入校验函数，POST/PUT 端点返回友好的参数错误信息。

4. **`main.py`**：添加 `finally` 块调用 `cleanup_parallel_agent()`，确保退出时释放线程池资源。

5. **`config/settings.py`**：API Key 缺失时抛出明确 `ValueError` 提示，而非静默传递 None。

6. **`platforms/platform_database.py`**：`query_product_by_attrs` 评分平局时按 `platform_price` 升序打破平局，优先返回价格更低的匹配。

7. **`.gitignore`**：添加 `debug_tool.py`，防止调试脚本误提交。

8. **`优化文档.md`**：修正"上下文支持"和"多工具并行"的状态标记，从错误的"已解决"更正为"待实现"。

---

### 7. 上下文管理（滑动窗口多轮对话）

**完成时间**：2026-05-11

**问题**：Agent 每次推理只能看到当前一条消息，无法理解"那小米14呢"、"这两个哪个更值得买"等上下文指代。

**解决思路**：

1. **`agent/react_engine.py`**：`run()` 新增 `history: Optional[List[Dict]]` 参数。新增 `_slide_window()` 方法，三层防护：
   - 过滤：只保留 user/assistant 角色（排除 tool/system 等 ReAct 中间产物）
   - 轮数截断：保留最近 6 轮（12 条消息）
   - 字符截断：总字符 ≤ 6000（约 3000-4000 token）
   - 从旧到新逐对移除，保证上下文连贯性

2. **`app.py`**：`/api/chat` 接口调用 `agent.run()` 前，从数据库读取 `get_session_messages()`，排除刚写入的当前消息，传入 `history` 参数。

3. **验证**：
   ```
   Q1: iPhone 15 哪个平台最便宜 → A1: 拼多多 ¥5750
   Q2: 那小米14呢            → A2: 淘宝 ¥3804  (理解"那"=最便宜平台)
   Q3: 这两个哪个更值得买      → A3: 小米14性价比更高 (理解"这两个"=iPhone+小米)
   ```

**解决状态**：✅ 已解决
