# IT3C 行业优化 — 问题复盘（产品经理视角）

> 记录时间：2026-05-12
> 项目阶段：price-agent IT3C 手机品类优化（Phase 1-5 全部完成）
> 最终评估通过率：46/46（100%）

---

## 前言

本文档从产品经理视角，记录 IT3C 优化实施过程中暴露的 7 个问题——它们不是在需求文档里能预测的，而是在代码落地和测试验证中才浮现的。每个问题都按照"发现 → 根因 → 解决 → 反思"的结构记录，目的是让后续类似项目少踩坑。

---

## Case 1：Schema 扩展不是"加字段"那么简单

### 发现过程

Phase 1-A 提示词里有一句："不要修改其他方法"。但实施时我意识到——当前所有方法都用 `row[0]` 到 `row[9]` 硬编码列索引。Schema 从 9 列加 8 个新列变成 17 列后，`SELECT *` 返回的列顺序变了，所有索引全部错位。

如果不修这些方法，`row[8]` 本来取的是 `color`，会变成取到 `brand`——颜色过滤彻底乱套。

### 根因

原始代码没有用列名映射，而是依赖 `SELECT *` 的隐式列顺序。这是早期快速迭代留下的技术债。

### 解决方案

1. 引入 `_COL` 类定义列名常量（`_COL.COLOR = 8`, `_COL.BRAND = 10` 等）
2. 抽取统一的 `_row_to_dict()` 函数，所有方法共用
3. 数据库初始化从 `CREATE TABLE IF NOT EXISTS` 改为 `DROP + CREATE`（因为是 mock 数据，无需保留）

### 产品反思

> **"加 8 个字段"听起来是 30 分钟的工作，实际上需要改 7 个方法的列索引映射，工作量 3-4 倍于预期。**
>
> 关键教训：当提示词说"不要改其他方法"时，需要从代码层面验证这个前提是否成立。如果底层依赖硬编码索引，那"不改其他方法"本身就是不可能的前提。

---

## Case 2：意图分类漏了处理器查询

### 发现过程

Phase 3-B 实现了意图分类，用 `USE_CASE_TRIGGER_MAP` 检测"游戏""拍照""学生"等场景词。但 Phase 5 写测试用例时，我发现 `"骁龙8Gen3手机有哪些"` 这条 query 不会被识别为 `recommendation`——因为"骁龙"不在触发词表里。

### 根因

需求文档只列了场景维度的触发词（gaming/photography/student 等），但遗漏了处理器维度（骁龙/天玑/麒麟/A17 等）。这是需求覆盖度的问题——没有穷举所有可能触发"推荐意图"的用户表达方式。

### 解决方案

1. 新增 `_PROCESSOR_TRIGGERS` 列表：覆盖骁龙、天玑、麒麟、A 系列、M 系列等关键词
2. 在 `_detect_intent` 的判断条件中加入 `has_processor` 分支

### 产品反思

> **"推荐型查询"不只是"推荐"这两个字——用户说"骁龙处理器手机有哪些"本质上也是推荐。**
>
> 做意图分类时，需要从用户表达方式倒推，而不是从功能标签正推。测试用例的设计应该先于代码实现，这样能在编码前发现覆盖度缺口。

---

## Case 3：性价比公式让小众产品排第一

### 发现过程

审查 Phase 3-A 的性价比评分公式 `tier/price * 10000` 时，用 mock 数据算了一下：

| 商品 | tier | 价格 | 性价比分 |
|------|------|------|---------|
| 红米Note13 | 35 (budget) | ¥1,299 | **269.4** 🥇 |
| 小米14 | 100 (flagship) | ¥3,999 | 250.1 🥈 |
| iPhone 15 Pro | 100 (flagship) | ¥8,999 | 111.1 🥉 |

红米Note13 排第一在数学上成立，但如果用户是"旗舰游戏手机推荐"，这个排序就反直觉。

### 根因

`tier/price` 是一个线性公式，低价产品的得分天然偏高。需求中没有定义"性价比"的内涵——是"每一分钱买到多少性能"还是"在某个性能级别内找最便宜的"。

### 解决方案

当前不修（标记为已知限制）。后续可加 `min_performance_tier` 参数，当用户提到"旗舰"时自动限定。

### 产品反思

> **"性价比"三个字在不同用户嘴里意思完全不同。对预算有限的学生，性价比=最便宜里最好的；对数码发烧友，性价比=旗舰里最值的。**
>
> 后续需求需要明确：让用户选择一个锚点（预算锚/性能锚/品牌锚），再在这个锚点内做排序。

---

## Case 4：非手机品类的数据填充被忽略

### 发现过程

设计 mock 数据时，平板和耳机也进了数据库，但它们的 processor/processor_brand/performance_tier 字段怎么填？需求文档只给了手机的例子。

### 根因

Field design from a mobile-phone-centric perspective, didn't account for cross-category products. 但推荐系统是全品类的（category 参数默认"手机"但可以改成"平板""耳机"），数据不全会导致这些品类在过滤时行为不一致。

### 解决方案

定义非手机品类的填充规则：
- iPad Pro：processor="M2"，pb="apple"，tier="flagship"
- 小米平板6：processor="骁龙870"，pb="sd"，tier="mid"
- AirPods Pro 2：processor=""，pb=""，tier="flagship"（耳机按品牌定位）

### 产品反思

> **Schema 设计时必须覆盖所有品类的边界值。** 如果只按手机品类定义字段含义，平板和耳机会成为"二等公民"——要么空值导致过滤跳过，要么乱填导致排序错误。

---

## Case 5：单商品 + "推荐"词 → 被误判为对比

### 发现过程

Phase 5 P0 测试时，`"推荐iPhone 15"` 被分类为 `comparison` 而不是 `query`。用户明明在问 iPhone 15 的价格（"推荐"只是口语前缀），但系统把它当成了"推荐型查询"然后因为识别到具体型号而跳到对比分支。

### 根因

意图路由逻辑有三个分支：recommendation → comparison → query。当 `has_specific_model=True` 时，`_detect_intent` 会跳过 recommendation 分支，但紧随其后的 `_is_complex()` 检测到"推荐"在 complexity_keywords 里，就把它判为 comparison。

这是两个分类器的叠加冲突——`_is_complex` 是旧版的复杂度判断，在意图分类引入后出现了不该触发的场景。

### 解决方案

新增路由规则：当 `model_count == 1`（恰好一个已知型号），直接返回 `query`，不再检查 `_is_complex`。同时把 `_has_specific_model` 升级为 `_count_models`，不仅能判断有无型号，还能判断有几个。

### 产品反思

> **新功能叠加在旧逻辑上时，边界 case 最容易出问题。** "推荐"这个词在 complexity_keywords 里是合理的（可以触发 Plan-Execute 做深度分析），但在意图分类引入后，它的语义发生了变化（变成了"推荐型 intent"的触发器）。
>
> 教训：引入新路由层时，需要审计旧路由层的触发词是否还适用。

---

## Case 6：DeepSeek 模型的 thinking tokens 导致 JSON 被截断

### 发现过程

Phase 5 P1 评估出现随机性失败——同一个 query 有时能正确解析，有时全部返回空值。通过逐步调试发现：模型返回的 `completion_tokens` 始终是 200（上限），但实际 JSON 内容为空。

进一步测试发现：`max_tokens=200` 时，模型用了 100+ tokens 做内部推理（thinking），留给实际输出的配额不到 100。当 prompt 较长时，thinking 消耗更多，实际输出被完全截断。

### 根因

DeepSeek V4 系列的 thinking 机制会把推理链计入 `max_tokens` 配额。而我们的 prompt（1500+ chars 的 few-shot 示例）已经很长，thinking tokens 很容易超过输出配额。

### 解决方案

`max_tokens` 从 200 → 500。修改后 P1 全部通过。

### 产品反思

> **切换模型不是换个 model name 就完事。** 不同模型的 token 分配策略完全不同。豆包模型的 thinking 不计入 max_tokens，但 DeepSeek 计入。这种差异不会在 API 文档里用红字标出来，只能通过实际的 token 用量监控发现。
>
> 教训：模型切换后必须重新运行完整的评估套件，尤其是结构化输出（JSON）的测试。

---

## Case 7：DROP+CREATE vs 幂等的取舍

### 发现过程

Phase 1 把 `init_platform_db` 从"CREATE IF NOT EXISTS + 检查行数跳过"改为"DROP + CREATE"。这是为了确保 Schema 变更后不会残留旧结构。

但原有 P0 测试 RG-03 验证"重启后价格持久化"，DROP+CREATE 把修改都重置了，测试失败。

### 根因

Mock 数据场景下，数据初始化有两种哲学：
- **幂等**：数据像生产库一样保留，适合手动修改后不想丢的场景
- **确定性**：每次重启都是干净状态，适合测试和 CI

我们选择了确定性，代价是"手动改过的数据重启后会丢"。

### 解决方案

接受 RG-03 失败。Mock 数据场景下，确定性 > 持久性。后续如果接入真实数据源，需要切换到幂等策略。

### 产品反思

> **测试数据的管理策略不是技术细节，是产品决策。** 如果 mock 数据也要像真实数据一样"修改后保留"，那 Schema 变更时就需要写 migration。如果 mock 数据可以重建，那 DROP+CREATE 最简单可靠。两种选择对应不同的使用场景——开发期用重建，稳定期用迁移。

---

## Case 8：混合意图查询——用户同时要推荐和对比怎么办

### 发现过程

用户在代码审查时直接问："如果是复杂 query 包含了 recommendation 和 comparison，你是怎么处理的？"

这个问题暴露了意图分类的设计盲区——之前的逻辑假设每条 query 只属于一种意图（推荐、查价、对比），但真实用户可能会说"推荐一款游戏手机，然后和 iPhone 15 对比一下"——这句话同时有推荐意图（游戏手机）和对比意图（和 iPhone 15 比）。

进一步测试发现了 4 种路由冲突场景。

### 根因

三个独立的分类机制在叠加时产生了非预期的交互：

1. **"便宜" 词的多义性**：`USE_CASE_TRIGGER_MAP` 把"便宜"映射到 budget 标签，但用户说"小米14 最便宜"时是查价意图不是推荐。关键词匹配无法区分语境。

2. **"推荐" 词的双重归属**：`"推荐"` 同时出现在 `has_recommend_word`（意图分类）和 `complexity_keywords`（复杂度判断）两个列表中，导致"推荐iPhone 15"被推荐词触发后又因为复杂词被判为 comparison。

3. **型号检测的短路逻辑**：`model_count == 1 → query` 这条规则太绝对，"推荐游戏手机再和 iPhone 15 对比"虽然只检测到 1 个已知型号，但查询本质是复杂的。

4. **无型号对比词的失配**："和苹果手机比较"——"苹果手机"不在 product hints 里（hints 是"iPhone"这样的英文品牌名），model_count=0，导致对比意图未被识别。

### 解决方案

重新设计路由逻辑为 5 条正交规则：

1. 排除价格查询句式的干扰（"最便宜"/"多少钱"等）
2. 从 complexity_keywords 移除"推荐"/"建议"，交还给意图分类
3. "推荐"+单型号+无其他限定 → query
4. 推荐触发 + `is_complex`（对比/多步骤） → comparison
5. 其他场景保持原规则

修复后 20 个测试 case 全部通过。

### 产品反思

> **意图分类的边界 case 不是代码逻辑问题，是产品定义问题。** "推荐iPhone 15"到底是想让系统推荐类似手机（推荐意图），还是帮查 iPhone 15 价格（查价意图）？这个判断不能靠代码猜，需要在产品层面做出决策，然后代码严格执行。
>
> 我把决策写成了从用户表达方式倒推的规则：仅含"推荐"/"建议"词+单型号 → 查价；含场景/预算/处理器限定+单型号 → 看是否也有对比证据。这个规则的准确性，取决于对用户行为模式的理解是否正确。后续如果有真实用户数据，应该用这些边界 case 做 A/B 验证。
>
> 另外，这次是用户直接问"你怎么处理的"而不是测试暴露的——**好的产品问题比好的测试用例更难预测**，因为后者只覆盖已知场景，前者来自对真实使用场景的直觉。

---

## 总结：8 个问题的分类

| 类型 | Case | 阶段 |
|------|------|------|
| **技术债暴露** | #1 硬编码列索引 | 实施前未评估 |
| **需求覆盖度** | #2 处理器触发词遗漏 | 测试时发现 |
| **产品定义模糊** | #3 性价比公式歧义 | 方案审查时发现 |
| **边界遗漏** | #4 非手机品类数据 | 实施时发现 |
| **逻辑叠加冲突** | #5 意图分类+复杂度判断 | 测试时发现 |
| **平台差异** | #6 DeepSeek thinking tokens | 评估时发现 |
| **设计取舍** | #7 DROP+CREATE vs 幂等 | 实施时暴露 |
| **用户场景驱动** | #8 混合意图路由 | 用户提问时暴露 |

### 如果重做，我会这样改进流程

1. **代码审计前置**：实施前扫描所有依赖列索引的代码，评估改动范围
2. **意图分类先写测试用例**：用测试用例驱动触发词覆盖度检查
3. **性价比公式先跑数据**：用真实 mock 数据验证排序结果是否符合预期
4. **模型切换后立即跑全量评估**：不要只看 API 连通性，要跑结构化输出测试
5. **品类覆盖度检查**：Schema 设计时列出所有品类的边界值填充规则